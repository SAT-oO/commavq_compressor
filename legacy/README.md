# Legacy Code

This directory holds code paths that are not part of the active submission
pipeline.

Kept here for reference only:

- `legacy/codec/` — older codec format helpers
- `legacy/runtime/` — older entropy runtime helpers
- `legacy/model/temporal.py` — previous temporal model
- `legacy/estimate_sample.py` — old sampling estimator
- `legacy/test/build_submission.py` — old submission helper
- `legacy/resource/dataset_download.py` and `legacy/resource/tokens.npy` —
  manual dataset/sample utilities

Canonical active docs:

- `README.md`
- `docs/QUICK_START.md`
- `docs/TECHNICAL_OVERVIEW.md`

