[package]
name = "video_compressor"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
rand = { version = "0.8", features = ["small_rng"] }

[[bin]]
name = "video_compressor"
path = "src/main.rs"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
